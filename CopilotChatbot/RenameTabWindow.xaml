<Window x:Class="CopilotChatbot.RenameTabWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Rename Tab" Height="186" Width="440" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource PageBrush}">
    <DockPanel>
        <!-- Footer -->
        <Border DockPanel.Dock="Bottom" Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrushModern}" BorderThickness="0,1,0,0" Padding="16,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="84" Margin="0,0,8,0" Click="Cancel_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Chr" Background="{DynamicResource ControlBrush}"
                                    BorderBrush="{DynamicResource BorderBrushModern}" BorderThickness="1" CornerRadius="8">
                                <TextBlock Text="Cancel" Foreground="{DynamicResource TextBrush}"
                                           FontWeight="SemiBold" FontSize="13"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,8" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Chr" Property="Background" Value="{DynamicResource SurfaceBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button x:Name="ActionButton" Width="84" Click="Rename_Click" Tag="Rename">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Chr" Background="{DynamicResource AccentBrush}"
                                    BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="8">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                                            VerticalAlignment="Center" Margin="0,8">
                                    <ui:SymbolIcon Symbol="Rename20" FontSize="14" Foreground="White" Margin="0,0,5,0" />
                                    <TextBlock Text="{TemplateBinding Tag}" Foreground="White" FontWeight="SemiBold" FontSize="13" />
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Chr" Property="Background" Value="#0856B3" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Chr" Property="Background" Value="#0648A0" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>

        <!-- Content -->
        <StackPanel Margin="18,16,18,0">
            <TextBlock x:Name="FieldLabelTextBlock" Text="Tab name" FontWeight="SemiBold" FontSize="13"
                       Foreground="{DynamicResource TextBrush}" Margin="0,0,0,7" />
            <TextBox x:Name="TitleTextBox" Height="34" Padding="9,0"
                     Background="{DynamicResource ControlBrush}" Foreground="{DynamicResource TextBrush}"
                     CaretBrush="{DynamicResource TextBrush}"
                     BorderBrush="{DynamicResource BorderBrushModern}" BorderThickness="1"
                     VerticalContentAlignment="Center" />
        </StackPanel>
    </DockPanel>
</Window>
